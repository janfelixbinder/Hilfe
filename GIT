Verzeichnis angelegt
mkdir Verzeichnis

cd Verzeichnis

git init

vorher muss auf GITHUB das repository eröffnet werden

git remote add origin https://github.com/janfelixbinder/Hilfe.git

Abholen vom Server (origin) zur lokalen Kopie (master)
git pull origin master

änderungen auf den Server übertragen
git push




#befehle für simulations verzeichnisse
rückgängig machen
git reset --hard
git clean -f -d .


#kopiert dateien und verzeichniss in ein neues arbeitsverzeichnis
git --work-tree=../rechterWinkel checkout HEAD -- .
